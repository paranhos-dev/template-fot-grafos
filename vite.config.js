import { defineConfig } from 'vite'

// base = nome do repositorio, necessario para o GitHub Pages de projeto
// (o site fica em https://paranhos-dev.github.io/template-fot-grafos/)
export default defineConfig({
  base: '/template-fot-grafos/',
})
